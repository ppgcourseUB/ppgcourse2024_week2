#Spanish

Parametro Theta:

-El primero R script (3_EDchopped...) genera un archivo CSV partiendo del  
archivo "*.loci" generado por programas como ipyrad.

- El segundo script (04_Prior...) usa como input el CSV que da el  
anterior paso y calcula la distribución de theta y sus  
respectivos 2.5-97.5 quantiles en base a tus datos empiricos. Luego  
solo hay que modificar (a ojo, jugando un poco) el parametro beta  
("b") de la distribución gamma inversa y calcular los 2.5-97.5  
quantiles en base a esa distribución y ver que los quantiles empiricos  
caen dentro de los de la distribución gamma. 

Deberiamos hacer exactamente lo mismo para estimar Tau...

#English

Parameter Theta:

-The first R script (3_EDchopped...) generates a CSV file from the "*.loci" file generated by programs like ipyrad.  
loci" file generated by programs like ipyrad.

- The second script (04_Prior...) uses as input the CSV from the previous step and calculates the distribution of the theta.  
previous step and calculates the distribution of theta and its respective  
respective 2.5-97.5 quantiles based on your empirical data. Then  
you only have to modify (by eye, playing a bit) the parameter beta  
("b") of the inverse gamma distribution and calculate the 2.5-97.5  
quantiles on the basis of that distribution and see that the empirical quantiles  
fall within those of the gamma distribution. 

We should do exactly the same for estimating Tau...
